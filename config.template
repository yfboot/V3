# 本地配置模板（敏感信息勿提交）
# 使用：复制为 config.local，填写后保存。config.local 已在 .gitignore，不会提交。
# 私有 npm registry（flow.py 中 npm install 使用，与下方 Nexus 仓库对应）
PRIVATE_REGISTRY=

# Nexus 私服（publish.py、clear_repository.py 共用）
NEXUS_BASE_URL=
NEXUS_REPOSITORY=
NEXUS_USERNAME=
NEXUS_PASSWORD=

# 是否跳过阶段1/2（首次跑完可改为 true，只做阶段3 循环）
# 0/false=不跳过，1/true=跳过
SKIP_PHASE1=false
SKIP_PHASE2=false
